export interface IUserProps {
    imagePath: string;
    name: string;
    position?: string;
    cardNumber?: string;
  }
  
  export interface IProgressBarProps {
    limit: number;
    usedLimit: number;
    spentMoney: number;
  }
